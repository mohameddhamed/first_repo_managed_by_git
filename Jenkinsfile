pipeline {
  agent any
  stages {
      stage("build") {
          steps {
            echo "building in progress..." 
          }
      }
      stage("test") {
          steps {
            echo "testing in progress..." 
          }
      }
      stage("deploy") {
          steps {
            echo "deploying in progress..." 
          }
      }
  }
}
